export type ProductDetail = {
  name: string;
  price: number;
  // id: number
};

export type SavedProductDetail = {
  name: string;
  price: number;
  id: number;
  createdAt: string;
  lastUpdated: string;
};
